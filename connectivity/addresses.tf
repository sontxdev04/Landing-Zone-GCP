# Static external IP addresses
# No static IPs currently allocated.
# All VMs use Cloud IAP for access — no external IPs required.
# Add addresses here when load balancers or other services need static IPs.
